package com.mithi.algorithms.graph;
/*
Create graph object
Pass graph to graph processing routine

 */
public class Paths {
    /**
    Paths(Graph graph, int source) : find paths in G from source s
    boolean hasPathTo(int v) : is there a path from s to v
    Iterable<Intger> pathTo(int  v) : path from source to v, null if no such  path
    **/
}
